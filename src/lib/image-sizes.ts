/** Featured project cards: image column is ~60% of content width on desktop. */
export const FEATURED_CARD_IMAGE_SIZES =
  "(max-width: 1024px) 100vw, 60vw";

/** Compact two-column project cards. */
export const COMPACT_CARD_IMAGE_SIZES =
  "(max-width: 768px) 100vw, 50vw";

/** Full-width case study hero within the 1120px content column. */
export const CASE_STUDY_HERO_IMAGE_SIZES =
  "(max-width: 1120px) 100vw, 1120px";
